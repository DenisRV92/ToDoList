import React from 'react';

let initialState = {
    monday: [
        {id: 1, url: '/monday'},
    ]
}
const ReducerMonday = (state = initialState) => {
    return state
};

export default ReducerMonday;